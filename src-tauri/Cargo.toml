[package]
name = "worldviewer"
version = "0.1.0"
description = "WorldViewer application foundation"
edition = "2021"

[lib]
# Keep the library name distinct from the Windows executable (official scaffold).
name = "worldviewer_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "openseeface-spike"
path = "src/bin/openseeface_spike.rs"

[[bin]]
name = "worldviewer"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
